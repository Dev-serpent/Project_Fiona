"""Fiona Local Pages API Server.

HTTP REST + WebSocket + SSE backend for the Fiona web frontend.
Imports existing Fiona Python modules directly and exposes them
via aiohttp routes.
"""

from __future__ import annotations

__version__ = "0.1.0"
